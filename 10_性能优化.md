## 第十章：性能优化

### 图片压缩优化

**方法1：使用 Smush 插件**
```
媒体 → Smush → 批量优化
```

**方法2：手动压缩**
```
工具：
- TinyPNG（在线压缩）
- Photoshop（保存为 Web 格式）
```

**图片格式建议：**
| 格式 | 适用场景 | 压缩率 |
|-----|---------|-------|
| WebP | 通用图片 | 高 |
| JPEG | 照片 | 中 |
| PNG | 透明图片 | 低 |

### 缓存配置

**WP Super Cache 设置：**

1. **启用缓存**
```
设置 → WP Super Cache → 启用
```

2. **缓存选项**
```
- 启用压缩
- 启用缓存预加载
- 设置缓存过期时间
```

3. **CDN 配置**
```
设置 → WP Super Cache → CDN
启用 CDN → 输入 CDN URL
```

### 数据库优化

**方法1：使用 WP-Optimize 插件**
```
WP-Optimize → 运行优化
- 清理垃圾评论
- 清理草稿
- 优化数据库表
```

**方法2：手动优化**
```sql
OPTIMIZE TABLE wp_posts;
OPTIMIZE TABLE wp_postmeta;
DELETE FROM wp_postmeta WHERE meta_key LIKE '%_transient_%';
```

### 代码优化

**1. 合并 CSS/JS 文件**
```
使用 Autoptimize 插件：
- 合并 CSS 文件
- 合并 JS 文件
- 压缩代码
```

**2. 延迟加载**
```
使用 WP Rocket 或 LiteSpeed Cache
- 图片延迟加载
- JS 延迟加载
```

### 服务器优化

**1. 选择合适的主机**
```
推荐：
- SiteGround（SSD + CDN）
- WP Engine（托管 WordPress）
- 阿里云（国内）
```

**2. PHP 版本**
```
推荐 PHP 8.0+
设置 → 系统状态 → 查看 PHP 版本
```

**3. 启用 Gzip 压缩**
```
在 .htaccess 添加：
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
</IfModule>
```

### 性能监控

**工具推荐：**
- Google PageSpeed Insights
- GTmetrix
- Pingdom Tools

**关键指标：**
| 指标 | 目标值 |
|-----|-------|
| 首屏加载时间 | < 2秒 |
| 完全加载时间 | < 5秒 |
| PageSpeed 分数 | > 90分 |

### 性能优化检查清单

- [ ] 图片压缩
- [ ] 启用缓存
- [ ] 数据库优化
- [ ] 代码压缩
- [ ] 延迟加载
- [ ] CDN 配置
- [ ] PHP 版本升级

---

**下一章：第十一章 WordPress 安全**