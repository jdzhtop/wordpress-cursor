## 第六章：页面管理

### 创建页面

**步骤 1：进入页面编辑器**
```
仪表盘 → 页面 → 新建页面
```

**步骤 2：填写页面内容**
```
标题：关于我们
内容：使用区块编辑器添加段落、图片、按钮等
```

**步骤 3：设置页面属性**

| 属性 | 设置 | 说明 |
|-----|-----|------|
| 模板 | 默认模板/首页模板/空白页 | 控制页面布局 |
| 父页面 | 无/选择父页面 | 创建层级结构 |
| 排序 | 数字排序（0, 1, 2...） | 数字越小越靠前 |
| 评论 | 允许/不允许 | 页面通常关闭评论 |

**页面 vs 文章对比：**

| 特性 | 页面 | 文章 |
|-----|------|------|
| 时间属性 | 无 | 有（发布日期） |
| 分类/标签 | 无 | 有 |
| RSS 订阅 | 不包含 | 包含 |
| 层级结构 | 支持父子页面 | 不支持 |
| 适用场景 | 关于我们、联系页面 | 博客文章、新闻 |

### 页面模板

**常见页面模板：**

| 模板 | 说明 | 适用场景 |
|-----|------|---------|
| 默认模板 | 标准页面布局（含侧边栏） | 普通页面 |
| 首页模板 | 专为首页设计 | 网站首页 |
| 空白页 | 无侧边栏、无页脚 | 着陆页、活动页 |
| 全宽模板 | 全屏宽度 | 产品展示页 |

**创建自定义页面模板实例：**
```php
<?php
/**
 * Template Name: 自定义全宽页面
 * Description: 无侧边栏的全宽页面
 */

get_header();
?>

<div id="primary" class="content-area fullwidth">
    <main id="main" class="site-main" role="main">
        <?php while ( have_posts() ) : the_post(); ?>
            <article id="post-<?php the_ID(); ?>" <?php post_class(); ?>>
                <header class="entry-header">
                    <?php the_title( '<h1 class="entry-title">', '</h1>' ); ?>
                </header>
                <div class="entry-content">
                    <?php the_content(); ?>
                </div>
            </article>
        <?php endwhile; ?>
    </main>
</div>

<?php get_footer(); ?>
```

**使用方法：**
```
1. 将代码保存为 page-fullwidth.php

2. 上传到主题目录
   主题目录路径：wp-content/themes/你的主题名称/
   
   实例：
   - 如果你使用的是 Astra 主题：
     wp-content/themes/astra/page-fullwidth.php
   
   - 如果你使用的是 Twenty Twenty-Four 主题：
     wp-content/themes/twentytwentyfour/page-fullwidth.php
   
   上传方式：
   - 方式1：FTP 上传（FileZilla 等）
   - 方式2：宝塔面板文件管理器
   - 方式3：主机商提供的文件管理器

3. 页面编辑 → 页面属性 → 模板 → 选择「自定义全宽页面」

**原理：** WordPress 会自动扫描主题目录下的 page-*.php 文件，
并将它们识别为自定义页面模板，在页面编辑器的「模板」下拉菜单中显示。
```

### 导航菜单管理

**步骤 1：创建菜单**
```
仪表盘 → 外观 → 菜单 → 创建新菜单
菜单名称：主菜单 → 点击「创建菜单」
```

**步骤 2：添加菜单项**
```
左侧可选项目：
- 页面：勾选页面 → 添加到菜单
- 文章：勾选文章 → 添加到菜单
- 自定义链接：输入 URL 和链接文本
- 分类目录：勾选分类 → 添加到菜单
```

**步骤 3：设置菜单位置**
```
菜单设置 → 显示位置 → 勾选「主菜单」
点击「保存菜单」
```

**菜单结构实例：**
```
主菜单
├── 首页
├── 关于我们
│   ├── 公司简介
│   └── 团队成员
├── 服务
│   ├── 服务一
│   ├── 服务二
│   └── 服务三
├── 博客
└── 联系我们
```

**添加下拉菜单：**
```
拖拽菜单项向右缩进，成为子菜单项
示例：将「公司简介」拖到「关于我们」下方并缩进
```

### 创建联系表单

**方法 1：使用 Contact Form 7 插件**

**步骤 1：安装插件**
```
仪表盘 → 插件 → 安装插件 → 搜索 "Contact Form 7"
安装 → 启用
```

**步骤 2：创建表单**
```
仪表盘 → 联系 → 添加新
表单名称：联系表单
```

**步骤 3：编辑表单代码**
```html
<label> 您的姓名 (必填)
    [text* your-name] </label>

<label> 您的邮箱 (必填)
    [email* your-email] </label>

<label> 主题
    [text your-subject] </label>

<label> 您的留言
    [textarea your-message] </label>

[submit "发送"]
```

**步骤 4：插入表单到页面**
```
1. 复制短代码：[contact-form-7 id="123" title="联系表单"]
2. 页面 → 编辑页面 → 添加短代码区块
3. 粘贴短代码
4. 更新页面
```

**方法 2：使用 WPForms 插件（拖拽式）**
```
1. 安装 WPForms 插件
2. WPForms → 添加新 → 选择「联系表单」模板
3. 拖拽字段编辑
4. 保存 → 插入到页面
```

### 页面层级结构

**父子页面关系实例：**
```
关于我们（父页面，ID: 10）
├── 公司简介（子页面，父页面 ID: 10）
├── 团队成员（子页面，父页面 ID: 10）
└── 发展历程（子页面，父页面 ID: 10）
```

**设置父页面：**
```
页面 → 编辑页面 → 右侧「页面属性」
父页面 → 选择「关于我们」
点击「更新」
```

**子页面 URL 结构：**
```
父页面：https://example.com/about/
子页面：https://example.com/about/team/
```

### 页面优化建议

| 优化项 | 说明 | 操作方法 |
|-------|------|---------|
| 使用合适的模板 | 根据页面类型选择模板 | 页面属性 → 模板 |
| 保持 URL 简洁 | 使用有意义的 URL | 设置 → 固定链接 → 文章名称 |
| 添加面包屑导航 | 显示页面层级 | 使用 Yoast SEO 或 Rank Math |
| 优化页面加载速度 | 压缩图片、启用缓存 | 第18章详细说明 |
| 添加 SEO 元信息 | 标题、描述、关键词 | 使用 SEO 插件 |
| 移动端适配 | 确保响应式设计 | 使用浏览器开发者工具测试 |

### 批量管理页面

**批量操作：**
```
页面 → 所有页面
勾选多个页面 → 批量操作 → 选择操作
- 编辑：批量修改属性
- 移至回收站：删除多个页面
- 发布：批量发布草稿
```

**快速编辑：**
```
页面 → 所有页面
悬停在页面上 → 点击「快速编辑」
可修改：标题、别名、日期、状态、模板、父页面
```

---

**上一章：[第五章 创建文章](./05_创建文章.md)**

**下一章：[第七章 主题选择与设置](./07_主题选择与设置.md)**