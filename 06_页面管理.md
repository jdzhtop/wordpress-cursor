## 第6章：页面管理

### 创建页面

**步骤 1：进入页面编辑器**
```
仪表盘 → 页面 → 新建页面
```

**步骤 2：填写页面内容**
```
标题：关于我们
内容：使用区块编辑器添加段落、图片、按钮等
```

**步骤 3：设置页面属性**

| 属性 | 设置 | 说明 |
|-----|-----|------|
| 模板 | 默认模板/首页模板/空白页 | 控制页面布局 |
| 父页面 | 无/选择父页面 | 创建层级结构 |
| 排序 | 数字排序（0, 1, 2...） | 数字越小越靠前 |
| 评论 | 允许/不允许 | 页面通常关闭评论 |

**页面 vs 文章对比：**

| 特性 | 页面 | 文章 |
|-----|------|------|
| 时间属性 | 无 | 有（发布日期） |
| 分类/标签 | 无 | 有 |
| RSS 订阅 | 不包含 | 包含 |
| 层级结构 | 支持父子页面 | 不支持 |
| 适用场景 | 关于我们、联系页面 | 博客文章、新闻 |

### 页面模板

**常见页面模板：**

| 模板 | 说明 | 适用场景 |
|-----|------|---------|
| 默认模板 | 标准页面布局（含侧边栏） | 普通页面 |
| 首页模板 | 专为首页设计 | 网站首页 |
| 空白页 | 无侧边栏、无页脚 | 着陆页、活动页 |
| 全宽模板 | 全屏宽度 | 产品展示页 |

### WordPress FSE 主题自定义页面模板避坑教程

---

#### 一、核心前提：什么是 FSE 块主题模板？

WordPress 块主题（如 twentytwentyfive）的模板，是用来定义页面整体结构的「HTML 文件」，你可以用它实现：
- 固定不变的内容（如联系我们、页脚信息）全局复用
- 统一的页面布局（全屏、两栏、带侧边栏等）
- 编辑器和前端渲染分离，避免内容冲突

**关键特性（新手最容易踩坑）**
1. **数据库优先机制**：只要在后台编辑器里保存过模板，模板会被写入数据库，之后本地文件修改不再生效。
2. **路径强规则**：模板文件必须放在 `templates/` 根目录，不支持子目录嵌套。
3. **结构校验严格**：模板必须包含 `wp:post-content` 块，否则编辑器会报错、发布失败。

---

#### 二、第一步：创建自定义模板（标准流程）

**1. 文件路径与命名规则**
- 必须路径：`wp-content/themes/你的主题名/templates/`
- 文件命名：只能用小写字母、数字、连字符，不能有中文/空格，例如 `page-contact.html`
- 禁止操作：不要在 `templates/` 下新建子文件夹存放模板文件，WordPress 无法识别

**2. 模板头部声明（必须写在文件最开头）**
这是 WordPress 识别模板的关键，格式不能错：
```html
<!--
Template Name: 联系我们页面
Template Post Type: page
-->
```
- `Template Name`：后台编辑器里显示的模板名称
- `Template Post Type`：声明模板支持的内容类型，`page` 表示仅页面可用，如需同时支持文章，写 `post, page`

**3. 完整模板代码（可直接复制）**
下面是「固定内容 + 可编辑区」的标准模板，不会出现解析错误/发布失败：
```html
<!--
Template Name: 联系我们页面
Template Post Type: page
-->

<!-- 加载主题页眉 -->
<!-- wp:template-part {"slug":"header","theme":"twentytwentyfive"} /-->

<!-- 主内容区：constrained 表示内容居中对齐 -->
<!-- wp:group {"tagName":"main","layout":{"type":"constrained"},"style":{"spacing":{"margin":{"top":"var:preset|spacing|60"}}}} -->
<main class="wp-block-group" style="margin-top:var(--wp--preset--spacing--60)">

    <!-- 模板固定内容：所有使用该模板的页面都会自动显示，编辑器无法修改 -->
    <!-- wp:heading {"textAlign":"center","level":1} -->
    <h1 class="wp-block-heading has-text-align-center">联系我们</h1>
    <!-- /wp:heading -->

    <!-- 两栏布局 -->
    <!-- wp:columns {"align":"wide"} -->
    <div class="wp-block-columns alignwide">
        <!-- 左侧联系信息 -->
        <!-- wp:column {"width":"50%"} -->
        <div class="wp-block-column" style="flex-basis:50%">
            <!-- wp:paragraph -->
            <p><strong>地址：</strong>北京市朝阳区xxx路xxx号</p>
            <!-- /wp:paragraph -->
            <!-- wp:paragraph -->
            <p><strong>电话：</strong>400-xxx-xxxx</p>
            <!-- /wp:paragraph -->
            <!-- wp:paragraph -->
            <p><strong>邮箱：</strong>contact@example.com</p>
            <!-- /wp:paragraph -->
        </div>
        <!-- /wp:column -->

        <!-- 右侧表单（Contact Form 7） -->
        <!-- wp:column {"width":"50%"} -->
        <div class="wp-block-column" style="flex-basis:50%">
            <!-- wp:shortcode -->
            [contact-form-7 id="123" title="联系表单"]
            <!-- /wp:shortcode -->
        </div>
        <!-- /wp:column -->
    </div>
    <!-- /wp:columns -->

    <!-- 关键：编辑器可编辑区，必须保留！ -->
    <!-- wp:post-content /-->

</main>
<!-- /wp:group -->

<!-- 加载主题页脚 -->
<!-- wp:template-part {"slug":"footer","theme":"twentytwentyfive"} /-->
```

---

#### 三、第二步：模板使用与生效

**1. 后台编辑器中使用模板**
1. 新建/编辑一个「页面」
2. 在右侧「模板」下拉菜单中，选择你创建的模板名称
3. 编辑器里会显示空白的 `wp:post-content` 区域，在这里添加页面专属内容（如工作时间、地图）
4. 点击「发布」，模板里的固定内容和编辑器内容会同时渲染到前端

**2. 本地文件修改后如何生效**
只要你在后台保存过模板，数据库会覆盖本地文件，修改本地文件不会生效，解决方法：
1. 进入 WordPress 后台 → 外观 → 编辑器
2. 打开你的模板文件，点击右上角「...」→「重置模板」
3. 重置后，模板会重新读取本地文件的内容，修改即可生效

---

#### 四、常见问题与解决方案

**问题 1：模板文件放在子目录里，后台不显示**
- 原因：WordPress 不递归搜索 `templates/` 下的子文件夹，模板文件必须直接放在 `templates/` 根目录
- 解决：将模板文件移动到 `templates/` 根目录，文件名改为 `page-contact.html` 这种规范格式

**问题 2：编辑器里显示内容，前端页面什么都不显示**
- 原因 1：模板里缺少 `wp:post-content` 块，编辑器内容无法渲染
- 原因 2：模板被写入数据库，本地文件修改不生效，前端显示的是数据库里的旧版本
- 解决：
  1. 检查模板文件，确保包含 `<!-- wp:post-content /-->`
  2. 进入后台编辑器，重置模板，让系统重新读取本地文件

**问题 3：出现「区块包含未预料的或无效的内容」报错**
- 原因 1：模板和编辑器内容冲突，模板里的固定块被编辑器识别为无效内容
- 原因 2：模板里的 HTML 代码不规范，缺少闭合标签或块注释格式错误
- 解决：
  1. 在编辑器里删除所有被标记为无效的区块，只保留 `wp:post-content` 区域
  2. 用教程里的标准模板代码替换，确保所有块都用 `<!-- wp:xxx /-->` 格式包裹

**问题 4：保存模板时提示「更新失败，未找到匹配 URL 和请求方式的路由」**
- 原因：安全插件/服务器 WAF 拦截了 FSE 的 REST API 请求
- 解决：
  1. 临时禁用安全插件（如 Wordfence、iThemes Security）和缓存插件
  2. 进入后台 → 设置 → 永久链接，点击「保存更改」刷新重写规则
  3. 改用直接修改本地文件 + 重置模板的方式，绕过后台编辑器保存

---

#### 五、进阶：避免模板冲突的最佳实践

1. **只选一种修改方式**：要么只用本地文件修改（永不后台保存），要么只用后台编辑器修改（不碰本地文件），两者不要混着来
2. **模板文件做好备份**：修改前先复制一份模板文件，避免出错后无法恢复
3. **Contact Form 7 短码要正确**：在后台「联系 → 联系表单」里复制真实表单的短码，确保 id 和表单一致，否则表单无法渲染
4. **优先使用可复用区块**：如果只是想固定部分内容，不用模板也可以，在编辑器里创建「可复用区块」，更稳定、不易出错

---

#### 附：可复用区块替代方案（新手推荐）

如果不想折腾模板文件，可以用可复用区块实现固定内容复用：
1. 新建页面，在编辑器里搭建好联系我们的内容（标题、两栏信息、表单）
2. 全选所有区块，点击右上角「...」→「创建可复用区块」，命名为「联系我们固定内容」
3. 以后新建页面时，直接插入这个可复用区块，就能一键添加所有内容，编辑器和前端都不会出现冲突问题

---

### 区块 vs 模板：核心概念解析

**🧩 区块（Blocks）**
就是 WordPress 里用来搭建内容的最小「积木块」。你现在看到的「H1 标题」就是一个区块，还有段落、图片、列表、按钮、表格这些，每一种都是一个独立区块。

| 特性 | 说明 |
|-----|------|
| 作用 | 拼凑组合成文章/页面的内容 |
| 特点 | 每个区块都可以单独编辑、移动、设置样式 |
| 示例 | 改字体大小、对齐方式、背景色等 |

**📄 模板（Templates）**
是 WordPress 用来定义页面整体结构的「框架」，相当于一个预设好的「页面骨架」。

| 特性 | 说明 |
|-----|------|
| 作用 | 决定页面整体结构（有没有侧边栏、全屏还是固定宽度、页眉页脚怎么显示） |
| 特点 | 给不同页面套用模板，快速做出风格不一样的页面 |
| 示例 | 博客列表页、单篇文章页、联系页、全屏落地页 |

**🔍 形象比喻**

| 类比 | 解释 |
|-----|------|
| 模板 | 像房子的户型图，决定了房子的整体结构（几室几厅、厨房在哪、有没有阳台） |
| 区块 | 像房子里的家具，你可以把沙发、床、桌子这些区块，放到户型图里的不同位置，组合成你想要的房间布局 |

---

### FSE 主题 vs 传统主题

| 特性 | 传统主题 | FSE 主题 |
|-----|---------|---------|
| 模板文件 | `page-*.php` | `templates/*.html` |
| 语法 | PHP + HTML | 区块注释语法 |
| 模板位置 | 主题根目录 | `templates/` 目录 |
| 编辑方式 | 代码编辑 | 可视化编辑 + 代码 |
| 样式定义 | style.css | theme.json 或内联样式 |
| 适用主题 | Astra、Flatsome 等 | Twenty Twenty-Four/Five 等 |

---

### 创建自定义页面模板步骤

**步骤 1：理解模板文件结构**

一个标准的 FSE 模板文件必须包含：

| 必需元素 | 说明 | 示例 |
|---------|------|------|
| **模板声明注释** | 声明这是 WordPress 模板 | `<!-- Template Name: 全屏页面 -->` |
| **页眉模板** | 加载主题头部 | `<!-- wp:template-part {"slug":"header"} /-->` |
| **内容区域** | 显示页面内容 | `<!-- wp:post-content /-->` |
| **页脚模板** | 加载主题底部 | `<!-- wp:template-part {"slug":"footer"} /-->` |

**⚠️ 常见错误：缺少模板声明**

如果没有 `Template Name` 声明，这个文件不会出现在「页面编辑器 → 模板选择」里，无法给页面套用这个模板。

**步骤 2：创建模板 HTML 文件**

```
文件路径：wp-content/themes/你的主题/templates/page-fullwidth.html

实例（Twenty Twenty-Five 主题）：
wp-content/themes/twentytwentyfive/templates/page-fullwidth.html
```

**⚠️ 重要提醒：不要直接修改主题自带的模板**

如果你直接修改了主题里的文件，当主题更新时，你的修改会被覆盖。

**✅ 正确做法：创建子主题（Child Theme）**

```
1. 在 wp-content/themes/ 下创建子主题目录
   例：twentytwentyfive-child/

2. 创建 style.css：
   /*
   Theme Name: Twenty Twenty-Five Child
   Template: twentytwentyfive
   */

3. 创建 functions.php 引入父主题样式：
   <?php
   add_action('wp_enqueue_scripts', function() {
       wp_enqueue_style('parent-style', get_template_directory_uri() . '/style.css');
   });

4. 将自定义模板放在子主题的 templates/ 目录下
```

**步骤 3：编写模板代码（区块语法）**

**❌ 错误示例（缺少必要结构）：**

```html
<!-- 错误：缺少模板声明、页眉、页脚、内容区域 -->
<h1>Hello World!</h1>
```

这会导致：
- 页面没有主题的导航栏、页脚
- 编辑器里无法正常使用区块编辑器
- 所有套用这个模板的页面都显示固定文字，无法修改

**✅ 正确示例（完整模板结构）：**

```html
<!--
Template Name: 全屏无侧边栏页面
Template Post Type: page
-->

<!-- wp:template-part {"slug":"header","theme":"twentytwentyfive","area":"header"} /-->

<!-- 全屏宽度的主内容区域 -->
<main class="wp-block-group alignfull" style="padding-top: 0; padding-bottom: 0;">
    <!-- wp:post-content {"layout":{"type":"default"}} /-->
</main>

<!-- wp:template-part {"slug":"footer","theme":"twentytwentyfive","area":"footer"} /-->
```

**⚠️ 核心要点：模板里必须包含 `<!-- wp:post-content /-->`**

这是古腾堡编辑器的占位符标记，表示「编辑器里添加的内容在这里显示」。缺少这行，编辑器内容和模板冲突，会导致发布失败。

**步骤 4：应用模板**

```
1. 保存文件到 templates/ 目录
2. 进入 WordPress 后台 → 外观 → 编辑器
3. 刷新页面（按 Ctrl + Shift + R 强制刷新）
4. 新建页面 → 页面属性 → 模板 → 选择「全屏无侧边栏页面」
```

---

### 常用区块说明

| 区块 | 功能 | 代码示例 |
|-----|------|---------|
| `wp:template-part` | 加载页眉/页脚模板 | `<!-- wp:template-part {"slug":"header"} /-->` |
| `wp:post-title` | 显示页面标题 | `<!-- wp:post-title /-->` |
| `wp:post-content` | 显示编辑器中添加的内容 | `<!-- wp:post-content /-->` |
| `wp:post-featured-image` | 显示特色图片 | `<!-- wp:post-featured-image /-->` |
| `wp:group` | 容器组 | `<!-- wp:group -->...<!-- /wp:group -->` |
| `wp:columns` / `wp:column` | 栏布局 | `<!-- wp:columns -->...<!-- /wp:columns -->` |
| `wp:heading` | 标题区块 | `<!-- wp:heading -->...<!-- /wp:heading -->` |
| `wp:paragraph` | 段落区块 | `<!-- wp:paragraph -->...<!-- /wp:paragraph -->` |

---

### 关键属性说明

| 属性 | 说明 |
|-----|------|
| `"align":"full"` | 全宽显示 |
| `"layout":{"type":"default"}` | 默认布局（全宽） |
| `"layout":{"type":"constrained"}` | 约束布局（固定宽度，带侧边栏） |
| `"area":"header"` | 标记为页眉区域 |
| `"area":"footer"` | 标记为页脚区域 |

---

### 文件命名规则

```
templates/page-fullwidth.html → 模板名称：Full Width Page
templates/page-landing.html → 模板名称：Landing
templates/page-contact.html → 模板名称：Contact
```

| 规则 | 说明 |
|-----|------|
| 只能用小写字母、数字、连字符 | ✅ page-fullwidth.html |
| 不能有中文、空格、特殊字符 | ❌ page fullwidth.html |
| 文件名要能清晰表达模板用途 | 建议用英文 |

---

### 创建"联系我们"页面模板实例

**⚠️ 核心原则：模板 vs 编辑器的职责分离**

| 职责 | 应该写在哪里 | 说明 |
|-----|------------|------|
| 页面结构（header、footer、内容区框架） | 模板文件 `templates/xxx.html` | 定义整体布局 |
| 具体内容（标题、段落、表单） | 编辑器里 | 用户可在编辑器中自由修改 |

---

**方案 A：模板只写结构，内容全部在编辑器里写（基础版）**

**✅ 正确模板代码：**

```html
<!--
Template Name: 联系我们页面（纯结构）
Template Post Type: page
-->

<!-- wp:template-part {"slug":"header","theme":"twentytwentyfive","area":"header"} /-->

<!-- wp:group {"tagName":"main","style":{"spacing":{"margin":{"top":"var:preset|spacing|60"}}},"layout":{"type":"constrained"}} -->
<main class="wp-block-group" style="margin-top:var(--wp--preset--spacing--60)">
    <!-- wp:post-content /-->
</main>
<!-- /wp:group -->

<!-- wp:template-part {"slug":"footer","theme":"twentytwentyfive","area":"footer"} /-->
```

**使用方式：**
1. 保存为 `templates/page-contact.html`
2. 后台 → 页面 → 新建页面 → 模板选「联系我们页面（纯结构）」
3. 在编辑器里添加所有内容（标题、地址、电话、表单）

---

**方案 B：模板里写固定内容 + 保留可编辑区（推荐，支持内容重用）**

**✅ 正确模板代码（可直接发布）：**

```html
<!--
Template Name: 联系我们页面（固定+可编辑）
Template Post Type: page
-->

<!-- wp:template-part {"slug":"header","theme":"twentytwentyfive","area":"header"} /-->

<!-- wp:group {"tagName":"main","style":{"spacing":{"margin":{"top":"var:preset|spacing|60"}}},"layout":{"type":"constrained"}} -->
<main class="wp-block-group" style="margin-top:var(--wp--preset--spacing--60)">

    <!-- wp:heading {"textAlign":"center","level":1} -->
    <h1 class="wp-block-heading has-text-align-center">联系我们</h1>
    <!-- /wp:heading -->

    <!-- wp:columns {"align":"wide"} -->
    <div class="wp-block-columns alignwide">

        <!-- wp:column {"width":"50%"} -->
        <div class="wp-block-column" style="flex-basis:50%">
            <!-- wp:paragraph -->
            <p><strong>地址：</strong>北京市朝阳区xxx路xxx号</p>
            <!-- /wp:paragraph -->
            <!-- wp:paragraph -->
            <p><strong>电话：</strong>400-xxx-xxxx</p>
            <!-- /wp:paragraph -->
            <!-- wp:paragraph -->
            <p><strong>邮箱：</strong>contact@example.com</p>
            <!-- /wp:paragraph -->
        </div>
        <!-- /wp:column -->

        <!-- wp:column {"width":"50%"} -->
        <div class="wp-block-column" style="flex-basis:50%">
            <!-- wp:shortcode -->
            [contact-form-7 id="123" title="联系表单"]
            <!-- /wp:shortcode -->
        </div>
        <!-- /wp:column -->

    </div>
    <!-- /wp:columns -->

    <!-- wp:post-content /-->

</main>
<!-- /wp:group -->

<!-- wp:template-part {"slug":"footer","theme":"twentytwentyfive","area":"footer"} /-->
```

**使用方式：**
1. 保存为 `templates/page-contact.html`
2. 后台 → 页面 → 新建页面 → 模板选「联系我们页面（固定+可编辑）」
3. 编辑器里是空的，在这里添加「本页专属内容」（如工作时间、地图等）
4. 点击发布 → 不会报错

**效果：**
- 所有用这个模板的页面：自动显示固定的联系信息（修改模板才会改变）
- 每个页面：可以在固定内容下方自由添加专属内容

---

**方案 C：使用模板部件（Template Part）存固定内容（更专业）**

**步骤 1：创建模板部件**

文件：`parts/contact-info.html`

```html
<!-- wp:heading {"textAlign":"center","level":1} -->
<h1 class="wp-block-heading has-text-align-center">联系我们</h1>
<!-- /wp:heading -->

<!-- wp:columns {"align":"wide"} -->
<div class="wp-block-columns alignwide">
    <!-- wp:column {"width":"50%"} -->
    <div class="wp-block-column" style="flex-basis:50%">
        <!-- wp:paragraph -->
        <p><strong>地址：</strong>北京市朝阳区xxx路xxx号</p>
        <!-- /wp:paragraph -->
        <!-- wp:paragraph -->
        <p><strong>电话：</strong>400-xxx-xxxx</p>
        <!-- /wp:paragraph -->
    </div>
    <!-- /wp:column -->
    <!-- wp:column {"width":"50%"} -->
    <div class="wp-block-column" style="flex-basis:50%">
        <!-- wp:shortcode -->
        [contact-form-7 id="123" title="联系表单"]
        <!-- /wp:shortcode -->
    </div>
    <!-- /wp:column -->
</div>
<!-- /wp:columns -->
```

**步骤 2：在模板中引用**

文件：`templates/page-contact.html`

```html
<!--
Template Name: 联系我们页面（部件引用）
Template Post Type: page
-->

<!-- wp:template-part {"slug":"header","theme":"twentytwentyfive","area":"header"} /-->

<!-- wp:group {"tagName":"main","style":{"spacing":{"margin":{"top":"var:preset|spacing|60"}}},"layout":{"type":"constrained"}} -->
<main class="wp-block-group" style="margin-top:var(--wp--preset--spacing--60)">

    <!-- wp:template-part {"slug":"contact-info","theme":"twentytwentyfive"} /-->

    <!-- wp:post-content /-->

</main>
<!-- /wp:group -->

<!-- wp:template-part {"slug":"footer","theme":"twentytwentyfive","area":"footer"} /-->
```

**特点：**
- 联系信息集中在一个部件文件中，方便多处复用
- 修改 `parts/contact-info.html` 即可更新所有使用该部件的页面

---

**❌ 错误写法示例（会导致发布失败）：**

```html
<!-- 错误：模板里有固定内容，但没有 wp:post-content -->
<!-- wp:heading {"textAlign":"center","level":1} -->
<h2 class="wp-block-heading has-text-align-center">联系我们</h2>  <!-- level=1 应该对应 <h1>，不是 <h2> -->
<!-- /wp:heading -->
<!-- wp:paragraph -->
<p>地址：北京市朝阳区xxx路xxx号</p>
<!-- /wp:paragraph -->
```

**为什么会发布失败？**
- 模板里有内容，编辑器里也有内容 → 双重内容冲突
- 缺少 `wp:post-content` 占位符 → 编辑器内容没地方放
- REST API 校验不通过 → 发布失败

---

**Contact Form 7 短码检查：**

```
1. 确保已安装并激活 Contact Form 7 插件
2. 后台 → 联系 → 联系表单
3. 复制真实短码，替换 id 和 title
   正确示例：[contact-form-7 id="456" title="联系我们表单"]
4. 错误的 id 会导致页面报错
```

### 常见问题排查

| 问题 | 原因 | 解决方案 |
|-----|------|---------|
| 模板没有显示 | 缺少 Template Name 声明 | 添加 `<!-- Template Name: 模板名称 -->` |
| 模板没有显示 | 文件放错目录 | 确保放在 `templates/` 目录下 |
| 编辑器内容不显示 | 缺少 `wp:post-content` | 添加 `<!-- wp:post-content /-->` |
| 页面没有导航栏/页脚 | 缺少 `wp:template-part` | 添加页眉和页脚模板引用 |
| 编辑器和前端效果不一致 | 模板里硬编码了内容 | 确保使用 `wp:post-content` 而不是手动写内容 |
| 主题更新后模板消失 | 直接修改了主题文件 | 使用子主题，在子主题里创建模板 |
| 样式异常 | 使用了硬编码像素值 | 使用主题预设间距变量如 `var:preset\|spacing\|60` |

---

### 子主题创建完整流程

如果需要深度自定义模板，建议创建子主题：

**1. 创建子主题目录结构：**

```
wp-content/themes/twentytwentyfive-child/
├── style.css
├── functions.php
└── templates/
    └── page-fullwidth.html
```

**2. 创建 style.css：**

```css
/*
 Theme Name:   Twenty Twenty-Five Child
 Template:     twentytwentyfive
 Description:  Twenty Twenty-Five 子主题，自定义全宽页面模板
*/

@import url('../twentytwentyfive/style.css');
```

**3. 创建 functions.php：**

```php
<?php
add_action('wp_enqueue_scripts', function() {
    wp_enqueue_style('child-style', get_stylesheet_uri(), array('parent-style'));
});
```

**4. 将模板文件放入 templates/ 目录：**

```
wp-content/themes/twentytwentyfive-child/templates/page-fullwidth.html
```

### 导航菜单管理

**步骤 1：创建菜单**
```
仪表盘 → 外观 → 菜单 → 创建新菜单
菜单名称：主菜单 → 点击「创建菜单」
```

**步骤 2：添加菜单项**
```
左侧可选项目：
- 页面：勾选页面 → 添加到菜单
- 文章：勾选文章 → 添加到菜单
- 自定义链接：输入 URL 和链接文本
- 分类目录：勾选分类 → 添加到菜单
```

**步骤 3：设置菜单位置**
```
菜单设置 → 显示位置 → 勾选「主菜单」
点击「保存菜单」
```

**菜单结构实例：**
```
主菜单
├── 首页
├── 关于我们
│   ├── 公司简介
│   └── 团队成员
├── 服务
│   ├── 服务一
│   ├── 服务二
│   └── 服务三
├── 博客
└── 联系我们
```

**添加下拉菜单：**
```
拖拽菜单项向右缩进，成为子菜单项
示例：将「公司简介」拖到「关于我们」下方并缩进
```

### 创建联系表单

**方法 1：使用 Contact Form 7 插件**

**步骤 1：安装插件**
```
仪表盘 → 插件 → 安装插件 → 搜索 "Contact Form 7"
安装 → 启用
```

**步骤 2：创建表单**
```
仪表盘 → 联系 → 添加新
表单名称：联系表单
```

**步骤 3：编辑表单代码**
```html
<label> 您的姓名 (必填)
    [text* your-name] </label>

<label> 您的邮箱 (必填)
    [email* your-email] </label>

<label> 主题
    [text your-subject] </label>

<label> 您的留言
    [textarea your-message] </label>

[submit "发送"]
```

**步骤 4：插入表单到页面**
```
1. 复制短代码：[contact-form-7 id="123" title="联系表单"]
2. 页面 → 编辑页面 → 添加短代码区块
3. 粘贴短代码
4. 更新页面
```

**方法 2：使用 WPForms 插件（拖拽式）**
```
1. 安装 WPForms 插件
2. WPForms → 添加新 → 选择「联系表单」模板
3. 拖拽字段编辑
4. 保存 → 插入到页面
```

### 页面层级结构

**父子页面关系实例：**
```
关于我们（父页面，ID: 10）
├── 公司简介（子页面，父页面 ID: 10）
├── 团队成员（子页面，父页面 ID: 10）
└── 发展历程（子页面，父页面 ID: 10）
```

**设置父页面：**
```
页面 → 编辑页面 → 右侧「页面属性」
父页面 → 选择「关于我们」
点击「更新」
```

**子页面 URL 结构：**
```
父页面：https://example.com/about/
子页面：https://example.com/about/team/
```

### 页面优化建议

| 优化项 | 说明 | 操作方法 |
|-------|------|---------|
| 使用合适的模板 | 根据页面类型选择模板 | 页面属性 → 模板 |
| 保持 URL 简洁 | 使用有意义的 URL | 设置 → 固定链接 → 文章名称 |
| 添加面包屑导航 | 显示页面层级 | 使用 Yoast SEO 或 Rank Math |
| 优化页面加载速度 | 压缩图片、启用缓存 | 第18章详细说明 |
| 添加 SEO 元信息 | 标题、描述、关键词 | 使用 SEO 插件 |
| 移动端适配 | 确保响应式设计 | 使用浏览器开发者工具测试 |

### 批量管理页面

**批量操作：**
```
页面 → 所有页面
勾选多个页面 → 批量操作 → 选择操作
- 编辑：批量修改属性
- 移至回收站：删除多个页面
- 发布：批量发布草稿
```

**快速编辑：**
```
页面 → 所有页面
悬停在页面上 → 点击「快速编辑」
可修改：标题、别名、日期、状态、模板、父页面
```

---

**上一章：[第5章 创建文章](./05_创建文章.md)**

**下一章：[第7章 主题选择与设置](./07_主题选择与设置.md)**