## 第十二章：高级主题开发

### 原理剖析：WordPress 模板层级

**核心概念：** WordPress 按照特定顺序查找模板文件，优先使用更具体的模板。

**模板层级流程图：**
```
首页请求
    ↓
front-page.php → home.php → index.php

文章请求
    ↓
single-{post_type}-{slug}.php → single-{post_type}.php → single.php → index.php

页面请求
    ↓
page-{slug}.php → page-{id}.php → page.php → index.php

分类请求
    ↓
category-{slug}.php → category-{id}.php → category.php → archive.php → index.php
```

**模板文件说明：**

| 文件 | 用途 | 优先级 |
|-----|-----|-------|
| index.php | 默认模板 | 最低 |
| single.php | 文章详情页 | 中等 |
| single-{slug}.php | 指定文章 | 最高 |
| page.php | 页面 | 中等 |
| archive.php | 归档页 | 中等 |

### 实战：创建自定义页面模板

**需求：创建一个全宽页面模板**

**Step 1：创建模板文件**
```
在主题目录创建 page-fullwidth.php
```

**Step 2：编写模板代码**
```php
<?php
/**
 * Template Name: 全宽页面
 * Description: 没有侧边栏的全宽页面
 */

get_header();
?>

<div id="primary" class="content-area fullwidth">
    <main id="main" class="site-main" role="main">
    
        <?php while ( have_posts() ) : the_post(); ?>
        
            <article id="post-<?php the_ID(); ?>" <?php post_class(); ?>>
                <header class="entry-header">
                    <?php the_title( '<h1 class="entry-title">', '</h1>' ); ?>
                </header>
                
                <div class="entry-content">
                    <?php the_content(); ?>
                    <?php
                    wp_link_pages( array(
                        'before' => '<div class="page-links">' . __( 'Pages:', 'your-theme' ),
                        'after'  => '</div>',
                    ) );
                    ?>
                </div>
                
                <?php if ( comments_open() || get_comments_number() ) : ?>
                    <?php comments_template(); ?>
                <?php endif; ?>
                
            </article>
            
        <?php endwhile; ?>
        
    </main>
</div>

<?php get_footer(); ?>
```

**Step 3：添加样式**
```css
.content-area.fullwidth {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 40px 20px;
}
```

**Step 4：使用模板**
```
页面编辑 → 页面属性 → 模板 → 选择「全宽页面」
```

### 实战：注册自定义文章类型

**原理：** 自定义文章类型允许创建除文章和页面之外的内容类型。

**Step 1：在 functions.php 中注册**
```php
function create_portfolio_post_type() {
    // 标签定义
    $labels = array(
        'name'               => __( '作品集', 'text-domain' ),
        'singular_name'      => __( '作品', 'text-domain' ),
        'menu_name'          => __( '作品集', 'text-domain' ),
        'name_admin_bar'     => __( '作品', 'text-domain' ),
        'add_new'            => __( '添加作品', 'text-domain' ),
        'add_new_item'       => __( '添加新作品', 'text-domain' ),
        'new_item'           => __( '新作品', 'text-domain' ),
        'edit_item'          => __( '编辑作品', 'text-domain' ),
        'view_item'          => __( '查看作品', 'text-domain' ),
        'all_items'          => __( '所有作品', 'text-domain' ),
        'search_items'       => __( '搜索作品', 'text-domain' ),
        'parent_item_colon'  => __( '父作品:', 'text-domain' ),
        'not_found'          => __( '没有找到作品.', 'text-domain' ),
        'not_found_in_trash' => __( '回收站中没有作品.', 'text-domain' )
    );
    
    // 参数配置
    $args = array(
        'labels'             => $labels,
        'public'             => true,
        'publicly_queryable' => true,
        'show_ui'            => true,
        'show_in_menu'       => true,
        'query_var'          => true,
        'rewrite'            => array( 'slug' => 'portfolio' ),
        'capability_type'    => 'post',
        'has_archive'        => true,
        'hierarchical'       => false,
        'menu_position'      => 5,
        'menu_icon'          => 'dashicons-portfolio',
        'supports'           => array( 'title', 'editor', 'thumbnail', 'excerpt', 'custom-fields' )
    );
    
    register_post_type( 'portfolio', $args );
}
add_action( 'init', 'create_portfolio_post_type' );
```

**参数说明：**

| 参数 | 说明 |
|-----|-----|
| `public` | 是否公开可见 |
| `has_archive` | 是否启用归档页 |
| `rewrite` | URL 重写规则 |
| `supports` | 支持的功能（标题、编辑器等） |
| `menu_icon` | 菜单图标 |

**Step 2：创建归档模板**
```
创建 archive-portfolio.php
```

```php
<?php get_header(); ?>

<div id="primary" class="content-area">
    <main id="main" class="site-main" role="main">
    
        <header class="page-header">
            <h1 class="page-title"><?php post_type_archive_title(); ?></h1>
        </header>
        
        <?php if ( have_posts() ) : ?>
        
            <div class="portfolio-grid">
                <?php while ( have_posts() ) : the_post(); ?>
                
                    <article id="post-<?php the_ID(); ?>" <?php post_class( 'portfolio-item' ); ?>>
                        <?php if ( has_post_thumbnail() ) : ?>
                            <div class="portfolio-thumbnail">
                                <a href="<?php the_permalink(); ?>">
                                    <?php the_post_thumbnail( 'medium' ); ?>
                                </a>
                            </div>
                        <?php endif; ?>
                        
                        <header class="entry-header">
                            <?php the_title( '<h2 class="entry-title"><a href="' . esc_url( get_permalink() ) . '">', '</a></h2>' ); ?>
                        </header>
                        
                        <div class="entry-summary">
                            <?php the_excerpt(); ?>
                        </div>
                    </article>
                    
                <?php endwhile; ?>
            </div>
            
            <?php the_posts_navigation(); ?>
            
        <?php else : ?>
            <?php get_template_part( 'template-parts/content', 'none' ); ?>
        <?php endif; ?>
        
    </main>
</div>

<?php get_sidebar(); ?>
<?php get_footer(); ?>
```

### 实战：自定义字段（Advanced Custom Fields）

**原理：** 自定义字段允许在文章/页面中添加额外的元数据。

**Step 1：安装 ACF 插件**
```
插件 → 添加新插件 → 搜索 "Advanced Custom Fields" → 安装 → 启用
```

**Step 2：创建字段组**
```
自定义字段 → 添加新字段组
名称：作品信息
```

**Step 3：添加字段**

| 字段名 | 类型 | 说明 |
|-----|-----|-----|
| 作品年份 | 数字 | 作品创作年份 |
| 客户名称 | 文本 | 客户名称 |
| 项目链接 | URL | 项目演示链接 |

**Step 4：设置显示位置**
```
规则：显示在 作品类型 == portfolio
```

**Step 5：在模板中调用**
```php
<?php
// 显示作品年份
echo '<p>年份：' . get_field('作品年份') . '</p>';

// 显示客户名称
echo '<p>客户：' . get_field('客户名称') . '</p>';

// 显示项目链接
$link = get_field('项目链接');
if ( $link ) {
    echo '<a href="' . esc_url($link) . '" target="_blank">查看项目</a>';
}
?>
```

### 主题函数文件详解

**functions.php 是主题的核心文件，用于添加功能。**

**1. 启用主题支持**
```php
function mytheme_setup() {
    // 启用特色图片支持
    add_theme_support( 'post-thumbnails' );
    
    // 启用标题标签
    add_theme_support( 'title-tag' );
    
    // 启用 HTML5 支持
    add_theme_support( 'html5', array( 'search-form', 'comment-form', 'comment-list', 'gallery', 'caption' ) );
    
    // 启用自定义背景
    add_theme_support( 'custom-background', apply_filters( 'mytheme_custom_background_args', array(
        'default-color' => 'ffffff',
        'default-image' => '',
    ) ) );
}
add_action( 'after_setup_theme', 'mytheme_setup' );
```

**2. 注册菜单**
```php
function mytheme_register_nav_menus() {
    register_nav_menus( array(
        'primary' => __( '主菜单', 'mytheme' ),
        'secondary' => __( '二级菜单', 'mytheme' ),
        'footer' => __( '页脚菜单', 'mytheme' ),
    ) );
}
add_action( 'init', 'mytheme_register_nav_menus' );
```

**3. 注册侧边栏**
```php
function mytheme_widgets_init() {
    register_sidebar( array(
        'name'          => __( '侧边栏', 'mytheme' ),
        'id'            => 'sidebar-1',
        'description'   => __( '添加小工具到侧边栏', 'mytheme' ),
        'before_widget' => '<aside id="%1$s" class="widget %2$s">',
        'after_widget'  => '</aside>',
        'before_title'  => '<h2 class="widget-title">',
        'after_title'   => '</h2>',
    ) );
    
    register_sidebar( array(
        'name'          => __( '页脚小工具区', 'mytheme' ),
        'id'            => 'footer-1',
        'description'   => __( '添加小工具到页脚', 'mytheme' ),
        'before_widget' => '<div id="%1$s" class="widget %2$s">',
        'after_widget'  => '</div>',
        'before_title'  => '<h3 class="widget-title">',
        'after_title'   => '</h3>',
    ) );
}
add_action( 'widgets_init', 'mytheme_widgets_init' );
```

### 响应式设计原理

**原理：使用 CSS 媒体查询根据屏幕尺寸应用不同样式。**

**媒体查询示例：**
```css
/* 默认样式（桌面端） */
.container {
    width: 1200px;
    margin: 0 auto;
}

/* 平板端（768px - 991px） */
@media (max-width: 991px) {
    .container {
        width: 95%;
        padding: 0 15px;
    }
}

/* 手机端（最大767px） */
@media (max-width: 767px) {
    .container {
        width: 100%;
        padding: 0 10px;
    }
    
    /* 移动端菜单 */
    .main-navigation {
        display: none;
    }
    
    .mobile-menu-toggle {
        display: block;
    }
}

/* 大屏幕（最小1200px） */
@media (min-width: 1200px) {
    .container {
        width: 1170px;
    }
}
```

### 主题开发检查清单

- [ ] 模板层级正确
- [ ] 自定义文章类型注册
- [ ] 特色图片支持
- [ ] 菜单注册
- [ ] 小工具区域
- [ ] 响应式设计
- [ ] 子主题开发
- [ ] 代码注释清晰
- [ ] 国际化支持（text-domain）

---

**上一章：[第十一章 WordPress安全](./11_WordPress安全.md)**

**下一章：[第十三章 WooCommerce电商基础](./13_WooCommerce电商基础.md)**