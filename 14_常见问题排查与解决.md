## 第十四章：常见问题排查与解决

### 白屏问题

**原因：**
- PHP 内存不足
- 主题/插件冲突
- PHP 语法错误

**解决步骤：**

```
1. 启用调试模式
   wp-config.php 添加：
   define('WP_DEBUG', true);
   define('WP_DEBUG_LOG', true);

2. 查看错误日志
   wp-content/debug.log

3. 禁用所有插件
   FTP → wp-content/plugins/ → 重命名目录

4. 切换默认主题
   FTP → wp-content/themes/ → 重命名当前主题目录

5. 增加内存限制
   wp-config.php 添加：
   define('WP_MEMORY_LIMIT', '256M');
```

### 404 错误

**原因：**
- 固定链接设置错误
- .htaccess 损坏
- 页面已删除

**解决步骤：**

```
1. 重置固定链接
   设置 → 固定链接 → 保存更改

2. 检查 .htaccess
   添加正确的重写规则

3. 检查 mod_rewrite
   联系主机服务商确认启用

4. 检查页面是否存在
   文章/页面 → 所有文章/页面
```

### 数据库连接失败

**原因：**
- 数据库配置错误
- 数据库服务器未运行
- 用户权限不足

**解决步骤：**

```
1. 检查 wp-config.php
   DB_NAME、DB_USER、DB_PASSWORD、DB_HOST

2. 验证数据库连接
   使用 phpMyAdmin 测试

3. 检查用户权限
   GRANT ALL ON database.* TO user@localhost;

4. 修复数据库表
   REPAIR TABLE wp_posts;
```

### 图片无法上传

**原因：**
- 文件权限错误
- PHP 上传限制
- 内存不足

**解决步骤：**

```
1. 检查权限
   chmod 755 wp-content/uploads

2. 增加 PHP 限制
   php.ini: upload_max_filesize = 64M

3. 检查图片格式
   确保为 JPG/PNG/GIF

4. 使用图片优化插件
   安装 Smush
```

### 插件安装失败

**原因：**
- 文件权限
- 服务器空间不足
- 网络问题

**解决步骤：**

```
1. 手动安装
   下载 ZIP → FTP 上传 → 解压

2. 检查服务器空间
   联系主机服务商

3. 检查 WordPress 版本兼容性
```

### 登录页面循环重定向

**原因：**
- URL 设置错误
- Cookie 问题
- 插件冲突

**解决步骤：**

```
1. 设置正确 URL
   wp-config.php:
   define('WP_HOME', 'http://example.com');
   define('WP_SITEURL', 'http://example.com');

2. 清除浏览器缓存和 Cookie

3. 禁用插件
   FTP → 重命名 plugins 目录
```

### 问题排查工具

| 工具 | 用途 |
|-----|-----|
| Query Monitor | 调试数据库查询 |
| Debug Bar | 显示调试信息 |
| Health Check | 检测问题 |

### 寻求帮助

```
1. 官方文档：wordpress.org/support
2. 支持论坛：wordpress.org/support/forums/
3. 主机服务商技术支持
4. 专业开发者
```

### 问题排查流程图

```
发现问题
    ↓
检查错误日志
    ↓
禁用插件
    ↓
切换主题
    ↓
修复代码/配置
    ↓
测试验证
```

---

