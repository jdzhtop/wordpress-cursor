## 第十四章：常见问题排查与解决

### 白屏问题

**原因：**
- PHP 内存不足
- 主题/插件冲突
- PHP 语法错误

**解决步骤：**

```
1. 启用调试模式
   wp-config.php 添加：
   define('WP_DEBUG', true);
   define('WP_DEBUG_LOG', true);
   define('WP_DEBUG_DISPLAY', false);

2. 查看错误日志
   wp-content/debug.log

3. 禁用所有插件
   FTP → wp-content/plugins/ → 重命名目录

4. 切换默认主题
   FTP → wp-content/themes/ → 重命名当前主题目录

5. 增加内存限制
   wp-config.php 添加：
   define('WP_MEMORY_LIMIT', '256M');
```

### 国内访问问题

**问题1：Google Fonts 导致加载缓慢**

**原因：** WordPress 默认加载 Google Fonts，国内无法访问。

**解决方法：**

**方法1：使用插件（推荐）**
```
插件 → 安装插件 → 搜索 "Disable Google Fonts"
安装 → 启用（自动禁用 Google Fonts）
```

**方法2：通过 functions.php**
```php
// 禁用 Google Fonts
function disable_google_fonts() {
    wp_dequeue_style('google-fonts');
}
add_action('wp_enqueue_scripts', 'disable_google_fonts', 999);
```

**方法3：替换为国内字体源**
```php
// 替换 Google Fonts 为国内镜像
function replace_google_fonts($url) {
    if (strpos($url, 'fonts.googleapis.com') !== false) {
        $url = str_replace('fonts.googleapis.com', 'fonts.geekzu.org', $url);
    }
    return $url;
}
add_filter('style_loader_src', 'replace_google_fonts');
```

**问题2：Gravatar 头像无法显示**

**原因：** Gravatar 服务器被墙。

**解决方法：**
```php
// 替换 Gravatar 为国内镜像
function replace_gravatar($avatar) {
    $avatar = str_replace(array('www.gravatar.com', '0.gravatar.com', '1.gravatar.com', '2.gravatar.com'), 'gravatar.geekzu.org', $avatar);
    return $avatar;
}
add_filter('get_avatar', 'replace_gravatar');
```

**问题3：WordPress 更新失败**

**原因：** 国内访问 wordpress.org 速度慢。

**解决方法：**
```
方法1：使用国内镜像源
- 宝塔面板 → 软件商店 → WordPress 一键部署

方法2：手动更新
1. 下载 WordPress 中文版：https://cn.wordpress.org/
2. 解压后覆盖上传（保留 wp-content 目录）
3. 访问 /wp-admin/upgrade.php 完成更新
```

### 支付相关问题

**问题1：支付宝支付成功但订单未更新**

**原因：** 异步通知 URL 无法访问或配置错误。

**解决步骤：**
```
1. 检查通知 URL 是否可访问
   浏览器访问：https://yourdomain.com/?wc-api=wc_alipay_notify

2. 检查 SSL 证书是否有效
   支付宝要求 HTTPS

3. 查看支付日志
   WooCommerce → 状态 → 日志

4. 在支付宝后台测试通知回调
   开放平台 → 应用 → 接口调试
```

**问题2：微信支付扫码后无响应**

**原因：** 支付授权目录配置错误或 API 证书问题。

**解决步骤：**
```
1. 检查支付授权目录
   微信商户平台 → 产品中心 → 开发配置
   确保添加：https://yourdomain.com/checkout/

2. 检查 API 证书是否上传
   插件设置 → 证书路径

3. 查看微信支付日志
   WooCommerce → 状态 → 日志

4. 测试沙箱环境
   切换为沙箱模式测试
```

### WooCommerce 常见问题

**问题1：购物车页面空白**

**原因：** 页面短代码缺失或主题冲突。

**解决步骤：**
```
1. 检查页面内容
   页面 → 购物车 → 确保包含 [woocommerce_cart]

2. 重置 WooCommerce 页面
   WooCommerce → 状态 → 工具 → 创建默认页面

3. 检查主题兼容性
   切换为 Storefront 主题测试
```

**问题2：虚拟商品仍显示运费**

**原因：** 未正确标记为虚拟商品。

**解决步骤：**
```
1. 编辑产品 → 产品数据
2. 勾选「虚拟」选项
3. 勾选「可下载」选项（如需要）
4. 保存更新

批量修改：
产品 → 所有产品 → 批量操作 → 编辑
产品类型 → 更改为「虚拟」
```

### 性能问题

**问题1：网站加载缓慢**

**排查步骤：**
```
1. 使用 Query Monitor 插件检查：
   - 数据库查询次数（应 < 100）
   - 页面加载时间（应 < 2秒）
   - PHP 内存使用（应 < 128MB）

2. 检查图片大小
   - 单张图片应 < 200KB
   - 使用 WebP 格式

3. 检查插件数量
   - 禁用不必要的插件
   - 选择轻量级插件

4. 启用缓存
   - 安装 WP Rocket 或 WP Super Cache
   - 配置 CDN
```

**问题2：数据库查询缓慢**

**解决步骤：**
```
1. 优化数据库表
   插件 → WP-Optimize → 运行优化

2. 清理无用数据
   - 删除文章修订版本
   - 清理垃圾评论
   - 删除过期临时选项

3. 添加数据库索引
   联系开发者优化查询
```

### 安全问题

**问题1：网站被黑客攻击**

**应急处理：**
```
1. 立即修改所有密码
   - WordPress 管理员密码
   - 数据库密码
   - FTP 密码

2. 扫描恶意文件
   Wordfence → 扫描 → 立即扫描

3. 恢复备份
   UpdraftPlus → 恢复 → 选择最近备份

4. 更新所有组件
   - WordPress 核心
   - 主题
   - 插件
```

**问题2：登录页面被暴力破解**

**解决步骤：**
```
1. 启用登录限制
   插件 → Limit Login Attempts Reloaded

2. 修改登录 URL
   插件 → WPS Hide Login

3. 启用双因素认证
   Wordfence → 登录安全 → 2FA

4. 限制 IP 访问
   .htaccess 添加：
   <Files wp-login.php>
       Order Deny,Allow
       Deny from all
       Allow from YOUR_IP
   </Files>
```

### 404 错误

**原因：**
- 固定链接设置错误
- .htaccess 损坏
- 页面已删除

**解决步骤：**

```
1. 重置固定链接
   设置 → 固定链接 → 保存更改

2. 检查 .htaccess
   添加正确的重写规则

3. 检查 mod_rewrite
   联系主机服务商确认启用

4. 检查页面是否存在
   文章/页面 → 所有文章/页面
```

### 数据库连接失败

**原因：**
- 数据库配置错误
- 数据库服务器未运行
- 用户权限不足

**解决步骤：**

```
1. 检查 wp-config.php
   DB_NAME、DB_USER、DB_PASSWORD、DB_HOST

2. 验证数据库连接
   使用 phpMyAdmin 测试

3. 检查用户权限
   GRANT ALL ON database.* TO user@localhost;

4. 修复数据库表
   REPAIR TABLE wp_posts;
```

### 图片无法上传

**原因：**
- 文件权限错误
- PHP 上传限制
- 内存不足

**解决步骤：**

```
1. 检查权限
   chmod 755 wp-content/uploads

2. 增加 PHP 限制
   php.ini: upload_max_filesize = 64M

3. 检查图片格式
   确保为 JPG/PNG/GIF

4. 使用图片优化插件
   安装 Smush
```

### 插件安装失败

**原因：**
- 文件权限
- 服务器空间不足
- 网络问题

**解决步骤：**

```
1. 手动安装
   下载 ZIP → FTP 上传 → 解压

2. 检查服务器空间
   联系主机服务商

3. 检查 WordPress 版本兼容性
```

### 登录页面循环重定向

**原因：**
- URL 设置错误
- Cookie 问题
- 插件冲突

**解决步骤：**

```
1. 设置正确 URL
   wp-config.php:
   define('WP_HOME', 'http://example.com');
   define('WP_SITEURL', 'http://example.com');

2. 清除浏览器缓存和 Cookie

3. 禁用插件
   FTP → 重命名 plugins 目录
```

### 问题排查工具

| 工具 | 用途 |
|-----|-----|
| Query Monitor | 调试数据库查询 |
| Debug Bar | 显示调试信息 |
| Health Check | 检测问题 |

### 寻求帮助

```
1. 官方文档：wordpress.org/support
2. 支持论坛：wordpress.org/support/forums/
3. 主机服务商技术支持
4. 专业开发者
```

### 问题排查流程图

```
发现问题
    ↓
检查错误日志
    ↓
禁用插件
    ↓
切换主题
    ↓
修复代码/配置
    ↓
测试验证
```

---

**上一章：[第十三章 WooCommerce电商基础](./13_WooCommerce电商基础.md)**

**下一章：[第十五章 从0到1上线国内虚拟电商项目](./15_从0到1上线国内虚拟电商项目.md)**