## 第二章：准备工作

### 选择合适的服务器

**服务器类型对比：**

| 类型 | 适合人群 | 预算 |
|-----|---------|-----|
| 虚拟主机 | 初学者/个人博客 | ¥50-200/年 |
| VPS | 中小企业 | ¥200-800/月 |
| 独立服务器 | 大型网站 | ¥1000+/月 |

**推荐服务商：**
- **SiteGround**：WordPress 官方推荐
- **Bluehost**：性价比高
- **阿里云**：国内首选
- **腾讯云**：稳定可靠

**服务器选择建议：**
1. **流量预估**：日均访问量 < 1000 → 虚拟主机
2. **技术能力**：新手 → 托管主机；有经验 → VPS
3. **预算**：有限预算 → 虚拟主机；长期项目 → VPS

### 注册域名

**步骤1：选择域名注册商**
- 阿里云域名
- 腾讯云域名
- GoDaddy

**步骤2：查询域名可用性**
```
示例：
输入 "myblog" → 检查 myblog.com/.cn/.net 是否可用
```

**步骤3：完成注册**
1. 填写注册信息
2. 选择注册年限（建议注册2年以上）
3. 完成支付

**域名选择技巧：**
- 简短易记（建议 6-12 字符）
- 避免连字符和数字
- 使用关键词
- 优先选择 .com 域名

### 安装 PHP 和 MySQL 环境

**WordPress 7.0 环境要求：**
- PHP ≥ 8.0
- MySQL ≥ 5.7 或 MariaDB ≥ 10.3
- HTTPS 支持
- 内存限制 ≥ 256MB

**本地开发环境（Windows）：**

**方法1：Laragon（推荐）**

**特点：**
- 一键安装 Apache、MySQL、PHP
- 自动配置虚拟主机
- 支持多个 PHP 版本
- 自带 Composer、Git

**安装步骤：**
1. 下载 Laragon：[laragon.org](https://laragon.org/)
2. 双击安装，一路 Next
3. 启动 Laragon（点击 Start All）

**方法2：XAMPP**
```
1. 下载 XAMPP
2. 安装并启动 Apache + MySQL
3. 访问 http://localhost
```

**服务器环境配置：**

1. **创建数据库**
```sql
CREATE DATABASE wp_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'wp_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON wp_db.* TO 'wp_user'@'localhost';
FLUSH PRIVILEGES;
```

**原理：** `utf8mb4` 支持完整的 Unicode 字符（包括emoji），是 WordPress 推荐的字符集。

2. **检查 PHP 版本**
```php
<?php phpinfo(); ?>
```

### 域名解析设置

**原理：** 将域名指向服务器IP，让用户输入域名时能找到你的网站。

**解析类型说明：**

| 记录类型 | 说明 | 示例值 |
|---------|-----|-------|
| A记录 | 将域名指向IPv4地址 | 123.123.123.123 |
| AAAA记录 | 将域名指向IPv6地址 | 2001:0db8:85a3:0000:0000:8a2e:0370:7334 |
| CNAME记录 | 将域名指向另一个域名 | example.com |
| MX记录 | 邮件服务器记录 | mail.example.com |
| TXT记录 | 文本记录（用于验证） | v=spf1 include:_spf.google.com ~all |

**设置步骤：**

1. **登录域名注册商后台**
```
以阿里云为例：
- 登录阿里云控制台
- 进入域名管理
- 选择要解析的域名
```

2. **添加解析记录**
```
- 记录类型：A
- 主机记录：@（顶级域名）或 www
- 记录值：服务器IP地址
- TTL：默认300秒
```

3. **验证解析**
```
命令行执行：
ping yourdomain.com
nslookup yourdomain.com
```

**注意事项：**
- 解析生效时间通常为5-30分钟
- 国内服务器需要先备案才能使用域名访问
- 建议同时添加 www 和 @ 记录

### SSL证书设置

**原理：** SSL证书用于加密网站数据传输，提升安全性和搜索引擎排名。

**获取SSL证书：**

**方法1：使用 Let's Encrypt（免费）**
```
使用 Certbot 自动获取和续期证书：
sudo certbot --apache -d yourdomain.com -d www.yourdomain.com
```

**方法2：使用主机商提供的SSL**
```
很多主机商提供免费SSL证书：
- SiteGround：自动配置Let's Encrypt
- 阿里云：免费SSL证书服务
- 腾讯云：免费SSL证书服务
```

**方法3：购买商业证书**
```
适用于大型企业或需要更高安全性的网站：
- DigiCert
- Symantec
- GlobalSign
```

**配置SSL证书：**

1. **在主机面板中启用SSL**
```
cPanel → SSL/TLS → 安装SSL证书
```

2. **配置HTTPS重定向**
```
在 .htaccess 添加：
RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
```

3. **更新网站URL**
```
后台 → 设置 → 常规
WordPress地址(URL)：https://yourdomain.com
站点地址(URL)：https://yourdomain.com
```

**SSL检查：**
```
使用工具验证SSL配置：
- SSL Labs Server Test
- 浏览器地址栏显示锁图标
```

### 环境检查清单

| 项目 | 要求 | 检查方法 |
|-----|-----|---------|
| PHP 版本 | ≥ 8.0 | phpinfo() |
| MySQL | ≥ 5.7 | phpMyAdmin |
| 内存限制 | ≥ 256M | phpinfo() |
| 上传限制 | ≥ 2MB | phpinfo() |
| 扩展 | mysqli, curl, gd | phpinfo() |
| 域名解析 | 正确指向服务器 | nslookup |
| SSL证书 | 已安装并生效 | 浏览器检查 |

### 本地开发环境快速搭建

**Step 1：安装 Laragon**
```
下载地址：https://laragon.org/download/
版本：Laragon Full 64-bit
```

**Step 2：启动服务**
```
双击 Laragon → 点击 Start All
```

**Step 3：创建站点**
```
右键 Laragon 图标 → Quick App → WordPress
输入站点名（如 myblog）→ 回车
```

**Step 4：访问网站**
```
浏览器输入：http://myblog.test
用户名：admin
密码：在命令行窗口显示
```

---

**下一章：[第三章 WordPress 安装](./03_WordPress安装.md)**