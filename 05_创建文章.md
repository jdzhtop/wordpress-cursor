## 第5章：创建文章

### 原理剖析：WordPress 文章系统

**文章 vs 页面：**
- **文章**：时间序列内容（博客文章、新闻），有分类、标签
- **页面**：静态内容（关于我们、联系页面），无时间属性

**数据存储原理：**
- 文章数据存储在 `wp_posts` 表
- 文章元数据存储在 `wp_postmeta` 表
- 分类/标签存储在 `wp_terms`、`wp_term_relationships` 表

### 进入文章编辑器

**方法1：仪表盘快捷入口**
```
仪表盘 → 快捷操作 → 写文章
```

**方法2：菜单导航**
```
左侧菜单 → 文章 → 写文章
```

### Gutenberg 区块编辑器原理

**核心概念：** Gutenberg 是 WordPress 5.0+ 引入的区块编辑器，将内容分解为独立的「区块」。

**区块类型及用途：**

| 区块 | 用途 | 原理 |
|-----|-----|-----|
| 段落 | 正文内容 | 生成 `<p>` 标签 |
| 标题 | H1-H6 标题 | 生成 `<h1>`-`<h6>` 标签 |
| 图片 | 插入图片 | 生成 `<img>` 标签，支持响应式 |
| 列表 | 有序/无序列表 | 生成 `<ul>`/`<ol>` 标签 |
| 引用 | 引用文本 | 生成 `<blockquote>` 标签 |
| 代码 | 代码块 | 生成 `<pre><code>` 标签 |
| 分隔符 | 分隔线 | 生成 `<hr>` 标签 |
| 按钮 | 可点击按钮 | 生成带样式的 `<a>` 标签 |

**添加区块：**
```
1. 点击 "+" 按钮（左上角或区块之间）
2. 搜索区块名称（如「图片」）
3. 选择需要的区块
```

### 实战：创建第一篇文章

**Step 1：填写标题**
```
标题：WordPress 7.0 新特性详解
```
**原理：** 标题会作为 `<h1>` 标签和页面标题，是 SEO 的核心。

**Step 2：添加段落内容**
```
WordPress 7.0 带来了许多令人兴奋的新功能，让我们一起来探索这些强大的更新。
```

**Step 3：添加图片**

1. 点击「+」→ 搜索「图片」→ 选择「图片」区块
2. 点击「上传」→ 选择本地图片（如 `wp7-features.jpg`）
3. 设置图片属性：
   - 对齐：居中
   - 尺寸：大
   - 链接：无链接
   - Alt 文本：WordPress 7.0 新功能

**原理：** Alt 文本用于屏幕阅读器和 SEO，必须填写。

**Step 4：添加列表**
```
点击「+」→ 搜索「列表」→ 选择「无序列表」
输入：
- 区块编辑器改进
- 性能提升
- 安全增强
```

**Step 5：添加引用**
```
点击「+」→ 搜索「引用」→ 选择「引用」区块
输入：WordPress 7.0 是迄今为止最快、最安全的版本。
```

### 文章设置详解

**右侧面板设置：**

**1. 状态与可见性**
- **状态**：草稿（未完成）/ 待审核（需审核）/ 已发布（公开）
- **可见性**：公开 / 私有（仅管理员可见）/ 密码保护
- **发布日期**：可设置未来时间定时发布

**2. 分类**
```
文章 → 分类 → 新建分类：技术文章
在文章编辑页面勾选「技术文章」
```

**3. 标签**
```
输入：WordPress, WordPress 7.0, 教程
按回车添加每个标签
```

**4. 特色图片**
```
点击「设置特色图片」→ 上传或选择图片
```
**原理：** 特色图片用于文章列表展示和社交分享预览。

### 发布流程

**保存草稿：**
```
点击右上角「保存草稿」或按 Ctrl + S
```

**预览文章：**
```
点击「预览」→ 在新窗口查看效果
```

**发布文章：**
```
1. 确认所有设置
2. 点击「发布」
3. 在弹出框中再次点击「发布」
```

### 实战：编辑已发布文章

**Step 1：找到文章**
```
文章 → 所有文章 → 找到要编辑的文章 → 点击标题
```

**Step 2：修改内容**
```
修改标题或内容
```

**Step 3：更新文章**
```
点击右上角「更新」按钮
```

### 文章 SEO 优化原理

**标题优化：**
- 长度：50-60 字符（避免被搜索引擎截断）
- 包含主关键词（如「WordPress 7.0 教程」）
- 吸引点击（使用数字、疑问词）

**内容优化：**
- 关键词自然融入，避免堆砌
- 使用语义化标题（H2-H6）
- 添加内部链接（链接到相关文章）
- 图片添加 Alt 文本
- 内容长度建议 1500 字以上

**WordPress 7.0 AI 辅助优化：**
```
1. 编辑文章 → 点击右上角 AI 图标
2. 选择功能：
   - 调整语气：正式/友好/专业
   - 生成摘要：自动生成文章摘要
   - 改进写作：优化表达和语法
3. 确认修改 → 应用
```

### 快捷键速查

| 快捷键 | 功能 |
|-------|-----|
| `Ctrl + S` | 保存草稿 |
| `Ctrl + P` | 预览 |
| `Ctrl + B` | 加粗 |
| `Ctrl + I` | 斜体 |
| `Ctrl + K` | 插入链接 |
| `Esc` | 关闭弹窗/取消选择 |

### 常见问题

**问题1：区块消失**
```
原因：区块格式错误或插件冲突
解决：切换到「代码编辑器」查看并修复 HTML
```

**问题2：图片不显示**
```
原因：文件权限错误或路径问题
解决：检查 wp-content/uploads 权限为 755
```

---

**上一章：[第4章 WordPress后台基础](./04_WordPress后台基础.md)**

**下一章：[第6章 页面管理](./06_页面管理.md)**