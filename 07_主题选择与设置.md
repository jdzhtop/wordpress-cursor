## 第七章：主题选择与设置

### 原理剖析：WordPress 主题系统

**主题本质：** WordPress 主题是一组文件，用于定义网站的外观和布局。

**主题文件结构：**
```
your-theme/
├── style.css          # 主样式文件（必须）
├── index.php          # 默认模板（必须）
├── functions.php      # 主题功能（可选）
├── header.php         # 头部模板
├── footer.php         # 底部模板
├── single.php         # 文章详情模板
├── page.php           # 页面模板
├── archive.php        # 归档模板
└── screenshot.png     # 主题预览图
```

**模板层级原理：**
```
1. 特定模板 → 通用模板 → 默认模板
示例：page-about.php → page.php → index.php
```

### 选择合适的主题

**主题类型对比：**

| 类型 | 特点 | 适合场景 |
|-----|-----|---------|
| 博客主题 | 简洁，阅读体验优先 | 个人博客 |
| 企业主题 | 专业，功能丰富 | 企业官网 |
| 电商主题 | WooCommerce集成 | 在线商店 |
| 杂志主题 | 多栏目，内容丰富 | 门户网站 |

**选择要点：**
- **响应式设计**：必须支持手机、平板、桌面
- **加载速度**：避免过度臃肿的主题
- **SEO友好**：语义化HTML结构
- **更新频率**：作者持续维护
- **支持服务**：有文档和支持渠道

### 安装主题

**方法1：后台安装（推荐）**
```
外观 → 主题 → 添加新主题
搜索主题名称（如 Astra）→ 安装 → 启用
```

**方法2：上传安装**
```
外观 → 主题 → 添加新主题 → 上传主题
选择 ZIP 文件 → 安装 → 启用
```

### 自定义主题

**自定义面板：**
```
外观 → 自定义
```

**可自定义项目及原理：**

| 设置项 | 说明 | 原理 |
|-----|-----|-----|
| 站点标识 | Logo、标题、描述 | 修改 `<header>` 区域 |
| 颜色方案 | 主色、背景色等 | 修改 CSS 变量 |
| 字体设置 | 标题、正文字体 | 修改 `font-family` |
| 菜单 | 导航菜单结构 | 修改导航区域 |
| 小工具 | 侧边栏、页脚内容 | 修改 `sidebar.php` |
| 首页设置 | 静态首页或博客 | 修改首页模板 |

### 实战：创建子主题

**为什么使用子主题：**
- **保护自定义代码**：父主题更新不会覆盖子主题修改
- **便于升级**：父主题可安全更新
- **保持整洁**：自定义代码集中管理

**Step 1：创建子主题目录**
```
wp-content/themes/twentytwentyfour-child/
```

**Step 2：创建 style.css**
```css
/*
Theme Name:   Twenty Twenty-Four Child
Theme URI:    http://example.com/twentytwentyfour-child/
Description:  Twenty Twenty-Four Child Theme
Author:       Your Name
Author URI:   http://example.com
Template:     twentytwentyfour
Version:      1.0.0
License:      GNU General Public License v2 or later
License URI:  http://www.gnu.org/licenses/gpl-2.0.html
Tags:         light, dark, two-columns, right-sidebar, responsive-layout
Text Domain:  twentytwentyfour-child
*/

/* 自定义样式 */
body {
    font-family: 'PingFang SC', 'Microsoft YaHei', sans-serif;
}

.site-title {
    color: #2c3e50;
}
```
**原理：** `Template: twentytwentyfour` 指定父主题，WordPress 会自动加载父主题样式。

**Step 3：创建 functions.php**
```php
<?php
function twentytwentyfour_child_enqueue_styles() {
    // 加载父主题样式
    $parent_style = 'twentytwentyfour-style';
    
    wp_enqueue_style( $parent_style, 
        get_template_directory_uri() . '/style.css' );
    
    // 加载子主题样式（优先级更高）
    wp_enqueue_style( 'child-style',
        get_stylesheet_directory_uri() . '/style.css',
        array( $parent_style ), // 依赖父主题样式
        wp_get_theme()->get('Version')
    );
}
// 在 wp_enqueue_scripts 钩子中加载样式
add_action( 'wp_enqueue_scripts', 'twentytwentyfour_child_enqueue_styles' );

// 添加自定义功能
function my_child_theme_features() {
    // 启用特色图片支持
    add_theme_support('post-thumbnails');
    
    // 注册自定义菜单
    register_nav_menus(array(
        'secondary-menu' => '二级菜单',
    ));
}
add_action('after_setup_theme', 'my_child_theme_features');
?>
```
**原理：** `wp_enqueue_style()` 确保样式按正确顺序加载。

**Step 4：启用子主题**
```
外观 → 主题 → 找到子主题 → 启用
```

### 实战：修改主题模板

**需求：在文章详情页添加作者信息**

**Step 1：复制父主题模板**
```
复制 wp-content/themes/twentytwentyfour/single.php 
到 wp-content/themes/twentytwentyfour-child/single.php
```

**Step 2：修改模板**
```php
<?php
get_header();

while ( have_posts() ) :
    the_post();
    ?>
    <article id="post-<?php the_ID(); ?>" <?php post_class(); ?>>
        <header class="entry-header">
            <?php the_title( '<h1 class="entry-title">', '</h1>' ); ?>
            
            <!-- 添加作者信息 -->
            <div class="author-info">
                <?php echo get_avatar( get_the_author_meta('ID'), 80 ); ?>
                <div class="author-details">
                    <span class="author-name"><?php the_author(); ?></span>
                    <span class="author-bio"><?php the_author_meta('description'); ?></span>
                </div>
            </div>
        </header>
        
        <div class="entry-content">
            <?php the_content(); ?>
        </div>
    </article>
<?php
endwhile;

get_footer();
?>
```

**Step 3：添加样式**
```css
.author-info {
    display: flex;
    align-items: center;
    margin: 20px 0;
    padding: 15px;
    background: #f8f9fa;
    border-radius: 8px;
}

.author-info img {
    border-radius: 50%;
    margin-right: 15px;
}

.author-name {
    display: block;
    font-weight: bold;
    color: #2c3e50;
}

.author-bio {
    display: block;
    font-size: 14px;
    color: #666;
}
```

### 常用主题推荐

**免费主题：**
- **Astra**：轻量级，高度可定制
- **GeneratePress**：快速，SEO友好
- **OceanWP**：多功能，适合各种网站
- **Twenty Twenty-Four**：官方默认主题

**付费主题：**
- **Divi**：可视化页面构建器
- **Avada**：全能主题，功能丰富
- **Enfold**：企业级主题

### 主题测试清单

- [ ] 响应式设计测试（手机、平板、桌面）
- [ ] 浏览器兼容性（Chrome、Firefox、Safari、Edge）
- [ ] 插件兼容性（Yoast SEO、WooCommerce等）
- [ ] 加载速度测试（Google PageSpeed）
- [ ] 移动端触摸交互测试

---

**下一章：[第八章 插件使用](./08_插件使用.md)**